sh echo "[scenario] mobility only - deterministic BSSID reassociation"

sh echo "[step 0] clean topology and normal forwarding"
sh python3 /mnt/shared/island_mode/sdn_mesh_controller.py reset
sh python3 /mnt/shared/island_mode/edge_mode_controller.py normal

sh echo "[step 1] force initial association: resp on ap2"
py resp.setPosition("20,40,0")
sh sleep 2
resp iw dev resp-wlan0 disconnect 2>/dev/null || true
sh sleep 1
resp iw dev resp-wlan0 connect zone2-responder 2437 02:00:00:00:08:00
sh sleep 2
resp iw dev resp-wlan0 link

resp ip neigh flush all
crit ip neigh flush all
resp curl --connect-timeout 2 --max-time 3 http://10.0.0.30:8000

sh echo "[step 2] move resp from ap2 to ap3"
py resp.setPosition("50,40,0")
sh sleep 2
resp iw dev resp-wlan0 disconnect 2>/dev/null || true
sh sleep 1
resp iw dev resp-wlan0 connect zone3-users 2437 02:00:00:00:09:00
sh sleep 2
resp iw dev resp-wlan0 link

resp ip neigh flush all
crit ip neigh flush all
resp curl --connect-timeout 2 --max-time 3 http://10.0.0.30:8000

sh echo "[step 3] move resp from ap3 to ap4"
py resp.setPosition("80,40,0")
sh sleep 2
resp iw dev resp-wlan0 disconnect 2>/dev/null || true
sh sleep 1
resp iw dev resp-wlan0 connect zone4-iot 2437 02:00:00:00:0a:00
sh sleep 2
resp iw dev resp-wlan0 link

resp ip neigh flush all
crit ip neigh flush all
resp curl --connect-timeout 2 --max-time 3 http://10.0.0.30:8000

sh echo "[step 4] move resp from ap4 to ap5"
py resp.setPosition("110,40,0")
sh sleep 2
resp iw dev resp-wlan0 disconnect 2>/dev/null || true
sh sleep 1
resp iw dev resp-wlan0 connect zone5-other 2437 02:00:00:00:0b:00
sh sleep 2
resp iw dev resp-wlan0 link

resp ip neigh flush all
crit ip neigh flush all
resp curl --connect-timeout 2 --max-time 3 http://10.0.0.30:8000

sh echo "[step 5] move resp back to ap2"
py resp.setPosition("20,40,0")
sh sleep 2
resp iw dev resp-wlan0 disconnect 2>/dev/null || true
sh sleep 1
resp iw dev resp-wlan0 connect zone2-responder 2437 02:00:00:00:08:00
sh sleep 2
resp iw dev resp-wlan0 link

resp ip neigh flush all
crit ip neigh flush all
resp curl --connect-timeout 2 --max-time 3 http://10.0.0.30:8000

sh echo "[scenario] mobility only done"
